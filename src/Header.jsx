import React from 'react';

// This component displays a static title for the application.
function Header() {
  return (
    <header className="app-header">
      <h1>Music Releases</h1>  {/* Displaying the static title using a h1 tag. */}
    </header>
  );
}

export default Header;  // Exporting the Header component to be used in other files.
